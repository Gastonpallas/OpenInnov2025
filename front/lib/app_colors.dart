import 'package:flutter/material.dart';

class AppColors {
  // Palette principale
  static const Color primary = Color(0xFFFF6EC7);       // Joy (Rose fuchsia)
  static const Color accent = Color(0xFFFFD93D);        // Fun (Jaune éclatant)
  static const Color secondary = Color(0xFF6A67CE);     // Playful (Violet pastel)
  static const Color support = Color(0xFF5CE1E6);       // Fresh (Turquoise clair)

  // Fonds
  static const Color background = Color(0xFFFFF6FB);    // Soft Pop (Rose pâle)

  // États
  static const Color success = Color(0xFF00C896);       // Progression (Vert menthe)
  static const Color alert = Color(0xFFFF5D5D);         // Manque (Rouge corail)

  // Texte
  static const Color textPrimary = Color(0xFF2B2D42);   // Texte principal (Bleu-gris foncé)
  static const Color textSecondary = Color(0xFF66687A); // Texte secondaire (Gris bleuté)
}
